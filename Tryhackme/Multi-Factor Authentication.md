**Multi-Factor Authentication (MFA)** is a security mechanism that requires users to verify their identity using **more than one authentication factor**.

Instead of relying only on a password, MFA adds **extra layers of protection**, making unauthorized access much harder.