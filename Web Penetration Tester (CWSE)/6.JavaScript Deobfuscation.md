**JavaScript deobfuscation** is the process of analyzing and reversing intentionally hidden or scrambled code.

It is an essential skill for:

- Code analysis
- Reverse engineering
- Malware investigation
- Red team and blue team operations

In real-world scenarios and CTF challenges:

- Attackers often use **obfuscated JavaScript** to hide malicious functionality.
- Malware may use obfuscated JS to download or execute its main payload.
- Without deobfuscating the code, it is difficult to understand its true behavior.

#### Why It Matters?

- Helps identify hidden malicious actions.
- Enables deeper understanding of web-based attacks.
- Critical for both:
    - **Red team** (analyzing exploit code)
    - **Blue team** (investigating and defending against attacks)

---
# Source Code

#### 1. Client-Side Source Code Basics

Modern websites use three main technologies:

- **HTML** → Defines structure (content, fields, layout)
- **CSS** → Defines styling (design, fonts, spacing)
- **JavaScript (JS)** → Handles functionality and logic

Even though this code runs in the browser, it is fully available to the client.

#### 2. Viewing HTML Source

- You can view page source using **CTRL + U**.
- This reveals:
    - HTML structure
    - Embedded CSS
    - JavaScript references
    - Developer comments

### Why Check HTML?

- Developers may leave:
    - Comments with hints
    - Hidden inputs
    - Sensitive information
- Always read HTML comments carefully during CTF.

#### 3. CSS

CSS can be:

✔ Internal CSS

```
<style>
h1 { font-size: 144px; }
p { font-size: 64px; }
</style>
```

✔ External CSS

```
<link rel="stylesheet" href="style.css">
```

✔ Inline CSS (directly on element)

```
<h1 style="color: red;">Hello</h1>
```

#### 4. JavaScript

JavaScript can also be:

✔ Internal JS

```
<script>
    // JS code here
</script>
```

✔ External JS

```
<script src="secret.js"></script>
```

✔ Inline JavaScript (inside element attributes)

```
<button onclick="alert('Clicked!')">Click Me</button>
```

#### 5. Obfuscated JavaScript

When opening `secret.js`, you see something like:

```
eval(function(p,a,c,k,e,d){...})
```

---
# Code Obfuscation

Before we start learning about `deobfuscation`, we must first learn about `code obfuscation`. Without understanding how code is obfuscated, we may not be able to successfully deobfuscate the code, especially if it was obfuscated using a custom obfuscator.

**Code obfuscation** is a technique used to make code:

- Hard for humans to read
- Difficult to reverse engineer
- But still fully functional for the machine

Important:

- Functionality stays the same
- Performance may slightly decrease
- Done automatically using obfuscation tools

#### 1. How Obfuscation Works

Obfuscators transform code by:

- Renaming variables to meaningless names
- Encoding strings
- Changing control flow
- Wrapping logic in decoding functions
- Using string dictionaries

Example Concept:

Instead of:

```
console.log("Hello");
```

It may become:

```
eval(function(p,a,c,k,e,d){e=function(c){return c};if(!''.replace(/^/,String)){while(c--){d[c]=k[c]||c}k=[function(e){return d[e]}];e=function(){return'\\w+'};c=1};while(c--){if(k[c]){p=p.replace(new RegExp('\\b'+e(c)+'\\b','g'),k[c])}}return p}('0.1("2 3");',4,4,'console|log|hello|world'.split('|'),0,{}))
```

Or even more complex with runtime decryption.

Note: The above type of obfuscation is known as "packing", which is usually recognizable from the six function arguments used in the initial function "function(p,a,c,k,e,d)".

#### 2. Why JavaScript is Commonly Obfuscated

Languages like:

- Python
- PHP

→ Usually run server-side  
→ Not visible to users

But:

JavaScript runs:

- Client-side (browser)
- Sent in cleartext to users

Because of this:

- Anyone can view source
- Anyone can inspect logic
- Anyone can copy code

So developers use obfuscation to hide logic.

#### 3. Use Cases of Obfuscation

Protect Intellectual Property:

- Prevent copying code
- Hide business logic
- Make reverse engineering harder

Add a Security Layer:

- Hide authentication logic
- Hide encryption routines

⚠ Important:  
Client-side authentication/encryption is NOT secure.  
Users can still:

- Read
- Modify
- Bypass it

Never trust client-side logic in real-world apps.

Malicious Usage (Most Common in Pentesting):

Attackers use obfuscation to:

- Bypass IDS/IPS systems
- Hide malicious payloads
- Evade antivirus detection
- Avoid signature-based detection

Common in:

- Malicious JavaScript
- Phishing pages
- Drive-by downloads
- Exploit kits

Resource[https://beautifytools.com/javascript-obfuscator.php]

---
# Basic Obfuscation

#### 1. Code Obfuscation Tools

- Obfuscation is usually done using automated tools.
- Many online obfuscators exist.
- Advanced attackers and professional developers often build custom obfuscators.
- Custom obfuscation = harder to reverse.

#### 2. JavaScript Minification

##### 🔹 What is Minification?

Minification:

- Removes spaces
- Removes line breaks
- Removes comments
- Keeps functionality the same

Example:

Original:

```
function log() {
	console.log("Hello world");
}
```

Minified:

```
function log(){console.log("Hello world");}
```

(Not much difference for 1 line, but big difference for large scripts.)

##### 🔹 Purpose of Minification

- Reduce file size
- Improve loading speed
- Slightly reduce readability

Minified files usually end with: .min.js

⚠ Minification is NOT strong obfuscation.  
It only compresses formatting.

#### 3. JavaScript Packing (Stronger Obfuscation)

Example packed code:

```
eval(function(p,a,c,k,e,d){...})

this is Packer Obfuscation
```

#### 4. How Packer Obfuscation Works

Recognizable by:

```
function(p,a,c,k,e,d)
```

That’s where the term **P.A.C.K.E.R** comes from.

It works by:

1. Converting words and symbols into a dictionary (array).
2. Replacing them with indexes.
3. Rebuilding the original code during execution.
4. Executing it using `eval()`.    

Example inside:

```
'Module|Deobfuscation|JavaScript|HTB|log|console'.split('|')
```

This means:

- All words were packed into a list.
- The function reconstructs them in correct order.
- Then `eval()` executes the rebuilt code.

---
# Advanced Obfuscation

#### 1. Why Advanced Obfuscation?

Basic packing still leaves:

- Cleartext strings visible
- Keywords like `console`, `log`
- Hints about functionality

Advanced obfuscation aims to:

- Hide all readable strings
- Encrypt string arrays
- Obfuscate control flow
- Make reverse engineering much harder

#### 2. Using obfuscator.io

When using **obfuscator.io**:

Important setting:

- Enable **String Array Encoding → Base64**

What this does:

- Converts strings into Base64
- Decodes them at runtime
- Hides readable text completely

#### 3. What Advanced Obfuscated Code Looks Like

Example structure:

```
var _0x1ec6 = ['Bg9N','sfrciePHDM...'];
(function(...){ ... })();
var _0x14f8 = function(...) { ... };
console[_0x14f8('0x0')](_0x14f8('0x1'));
```

Characteristics:

- Random variable names
- Base64 encoded strings
- Self-invoking functions
- String decoder function
- Array rotation logic
- Runtime string reconstruction

You can no longer see:

- `"HTB"`
- `"JavaScript"`
- `"log"`
- `"console"`

Everything is hidden.

#### 4. How It Still Works

Execution flow:

1. Encoded strings stored in array
2. Decoder function defined
3. Base64 decoded at runtime
4. Strings reconstructed
5. Final call becomes:

```
console.log("HTB JavaScript Deobfuscation Module");
```

But only after runtime decoding.

#### 4. Performance Impact

Advanced obfuscation:

- Adds extra decoding functions
- Adds loops
- Adds runtime reconstruction

Result:

- Slower execution
- More CPU usage
- Larger file size

Obfuscation trades performance for secrecy.

#### 5. Extreme Obfuscation (JSFuck Style)

Example:

```
# javascript
[][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]][([][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]]+[])[!+[]+!+[]+!+[]]+(!![]+[][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]])[+!+[]+[+[]]]+([][[]]+[])[+!+[]]+(![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[+!+[]]+([][[]]+[])[+[]]+([][(!
...SNIP...
[]]+(!![]+[][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]])[+!+[]+[+[]]]+([][[]]+[])[+!+[]]+(![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[+!+[]]+([][[]]+[])[+[]]+([][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[][(![]+[])[+[]]+([![]]+[][[]])[+!+[]+[+[]]]+(![]+[])[!+[]+!+[]]+(!![]+[])[+[]]+(!![]+[])[!+[]+!+[]+!+[]]+(!![]+[])[+!+[]]])[+!+[]+[+[]]]+(!![]+[])[+!+[]]])[!+[]+!+[]+[+[]]]](!+[]+!+[]+[+[]])))()
```

Characteristics:

- Uses only:
    - `[]`
    - `!`
    - `+`
    - `()`
- No readable letters
- Very long expressions
- Extremely confusing

It still works because JavaScript:

- Converts booleans to strings
- Uses type coercion
- Builds characters dynamically

---
# Deobfuscation

Deobfuscation is the process of converting **obfuscated JavaScript code** back into readable and understandable code.

In CTFs and real-world apps, developers may:

- Minify code (compress into one line)
- Obfuscate variables and logic
- Pack code using `eval()` functions
- Encode strings (Base64, Hex, etc.)

Your goal as a pentester:

- Understand what the script is doing
- Identify hidden endpoints
- Extract secrets (flags, tokens, API keys)
- Find vulnerabilities

you can use this tools to do deobfuscation:

[https://beautifier.io/]
[https://prettier.io/playground/]
[https://matthewfl.com/unPacker.html]

#### Reverse Engineering

When automated tools fail:
You must manually analyze.

### Situations:

- Custom obfuscator used
- Strings encoded multiple times
- Functions nested deeply
- Variables renamed randomly

#### Manual Reverse Engineering Steps

1. Beautify first
2. Rename variables manually
    - a → user
    - b → token
3. Replace encoded strings
4. Remove junk code
5. Add console.log to inspect values
6. Use browser debugger breakpoints

---
# Code Analysis

After deobfuscation, we got:

```
'use strict';
function generateSerial() {
  var xhr = new XMLHttpRequest;
  var url = "/serial.php";
  xhr.open("POST", url, true);
  xhr.send(null);
};
```

- **What it does:**
    1. Creates an AJAX request (`XMLHttpRequest`)
    2. Targets `/serial.php` on the same domain
    3. Sends a **POST request** with **no data**
    4. Doesn’t handle the response


- **Why it matters in pentesting/CTFs:**
    - Hidden or unused developer functionality
    - Unreleased endpoints may have bugs or vulnerabilities
    - Could be exploited if the backend lacks proper access control


- **Next steps for testing:**
    1. Access `/serial.php` directly
    2. Send manual POST requests via browser, curl, or Burp Suite
    3. Check for:
        - Unauthorized access
        - Debug info
        - Hidden features or flags


**Key concept:**  Hidden JS endpoints + unprotected backend = potential **broken access control** or **logic flaw vulnerabilities**.

---
# HTTP Requests

Previously, we discovered that the JavaScript function sends:

```
POST /serial.php
```

Now, instead of relying on the browser, we replicate this manually using **cURL**.

#### Sending a Basic Request

```
Aj4y@htb[/htb]$ curl http://SERVER_IP:PORT/

</html>
<!DOCTYPE html>

<head>
    <title>Secret Serial Generator</title>
    <style>
        *,
        html {
            margin: 0;
            padding: 0;
            border: 0;
...SNIP...
        <h1>Secret Serial Generator</h1>
        <p>This page generates secret serials!</p>
    </div>
</body>

</html>
```

This returns the HTML source of the page.

#### Sending POST Data

```
Aj4y@htb[/htb]$ curl -s http://SERVER_IP:PORT/ -X POST -d "param1=sample"
```

---
# Decoding

After sending:

```
POST /serial.php

we received:
ZG8gdGhlIGV4ZXJjaXNlLCBkb24ndCBjb3B5IGFuZCBwYXN0ZSA7KQo=
```

This looks random — but it's **encoded**, not encrypted.

#### Common Encodings:

###### 1. Base64

How to Spot It:

- Only letters (A–Z, a–z)
- Numbers (0–9)
- `+` and `/`
- Often ends with `=` (padding)
- Length is multiple of 4

```
Encode:
echo text | base64

Decode:
echo encoded_string | base64 -d
```

###### 2. Hex Encoding

Each character → converted to ASCII hex value.

```
a → 61
b → 62
```

```
Encode:
echo text | xxd -p

Decode:
echo hex_string | xxd -p -r
```

When testing endpoints like:

```
/serial.php
```

The server may:

- Return Base64 flag
- Return encoded API key
- Return hidden message

HTB{3v3r_run_0bfu5c473d_c0d3!}